name: config_cat
description: A new Flutter project.
version: 0.0.1
author:
homepage:

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  quiver: '>=2.0.0 <3.0.0'
  logging: ^0.11.3+2
  dio: ^2.1.0
  json_annotation: ^2.0.0
  equatable: ^0.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^1.0.0
  json_serializable: ^2.0.0



